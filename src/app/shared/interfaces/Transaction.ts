export interface Transaction {
  // accountNo: string;
  // name: string;
  transTime: string;
  transType: string;
  refAccount: string;
  transAmount: number;
  remainingBalance: number;
  transId: number;
}
